# c++类型擦除技术

## 可调用对象的类型擦除技术(包装任务对象为统一的形式void() )

1. 直接接受可调用对象的地方可以用可变参数函数模版 + std::bind 绑定参数为空参数初始化function\<ret(void)\>,再用lambda表达式 作为适配器将返回值也统一为void,接着可以用它初始化packged_task<void()> task对象，task对象就是最终被包装和使用的任务类型，
