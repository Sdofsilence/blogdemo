# 迭代器和引用失效总结

[cppreference迭代器和引用失效总结](https://cppreference.cn/w/cpp/container)

迭代器的本质是指针，内存的修改或者移动，迭代器失效。

* 所以很简单的结论就是，list,forward_list,set,multiset,map,multimap这些用指针连接的非连续容器，增删不会导致迭代器和引用失效。

* 而vector,deque,string这些用索引连接的连续容器，增删会改变索引，导致部分迭代器有条件的失效。